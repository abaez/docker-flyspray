db:
  image: tutum/mariadb
  volumes:
    - ./db:/var/lib/mysql
  environment:
    - MARIADB_PASS=somepass

web:
  build: .
  ports:
    - "80:80"
  links:
    - db
  volumes:
    - ./app/cache:/app/cache
    - ./app/attachments:/app/attachments
