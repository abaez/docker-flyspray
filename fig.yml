web:
  build: .
  command: php -S 0.0.0.0:80 -t /app
  ports:
    - "80:80"
  links:
    - db
  volumes_from:
    - db

db:
  image: tutum/mariadb
  volumes:
    - /var/lib/mysql
  
  environment:
    - MARIADB_PASS=somepass
